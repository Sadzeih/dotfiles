local options = {
  ensure_installed = { "lua", "go", "gomod", "gosum", "gowork", "yaml", "html", "dockerfile", "gitcommit", "gitignore", "json", "terraform" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
